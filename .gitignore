# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Secrets
.env
.env.*

# Weekly snapshots — regenerated on every run. The league-wide player pool is
# ~200KB per gameweek; per-team picks are small but still local state.
# The directory itself is tracked via data/.gitkeep.
data/players/
data/teams/
data/snapshot_gw*.json

# macOS / editors
.DS_Store
.idea/
.vscode/
